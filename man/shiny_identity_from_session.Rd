% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth_helpers.R
\name{shiny_identity_from_session}
\alias{shiny_identity_from_session}
\title{Extract identity from a Shiny session's HTTP request.}
\usage{
shiny_identity_from_session(session)
}
\arguments{
\item{session}{A Shiny session object.}
}
\value{
A claims list (see [verify_shiny_jwt]) or NULL if unauthenticated.
}
\description{
Call this at session start (e.g., inside the Shiny server function) to
populate `session$userData$identity` from the verified JWT.
}
\examples{
\dontrun{
# In R/app_server.R, inside the server function:
identity <- shiny_identity_from_session(session)
session$userData$identity <- identity
if (is.null(identity)) {
  # No valid auth — show login required UI, OR fall back to free tier
} else if (identity$tier == "pro") {
  # Unlock pro features
}
}
}
