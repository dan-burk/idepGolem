# `npm version` (run in electron/) creates the git tag as desktop-v<version>
# instead of the default v<version>. This keeps desktop-app releases on their
# own tag namespace, separate from the web app's v2.x tags, and matches the
# tag prefix the build-electron-* GitHub workflows trigger on.
tag-version-prefix=desktop-v
